{
  "name": "sc4053-project",
  "version": "1.0.0",
  "scripts": {
    "dev": "next dev -p 5173",
    "build": "next build",
    "start": "next start -p 5173",
    "compile": "hardhat compile",
    "clean": "hardhat clean",
    "node": "hardhat node --hostname 127.0.0.1 --port 8545",
    "deploy:mock": "hardhat ignition deploy ./ignition/modules/MockStablecoins.js --network localhost",
    "deploy:dex": "hardhat ignition deploy ./ignition/modules/Dex.js --network localhost",
    "mint": "hardhat run --network localhost scripts/mintTokensToAccount.js",
    "seed": "hardhat run --network localhost scripts/seedOrders.js",
    "balance": "hardhat run --network localhost scripts/checkBalances.js", 
    "order": "hardhat run --network localhost scripts/checkOrders.js",
    "test": "hardhat test",
    "get:addresses": "hardhat run --network localhost scripts/addresses.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "dotenv": "^17.2.3",
    "hardhat": "^2.26.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "node": "^22.17.1",
    "typescript": "^5.8.3",
    "viem": "^2.33.0",
    "vite": "^7.0.5"
  }
}
